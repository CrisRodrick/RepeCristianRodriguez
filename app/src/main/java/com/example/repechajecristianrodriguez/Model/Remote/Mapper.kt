package com.example.repechajecristianrodriguez.Model.Remote

import com.example.repechajecristianrodriguez.Model.Local.Entities.LocalDetails
import com.example.repechajecristianrodriguez.Model.Local.Entities.LocalList
import com.example.repechajecristianrodriguez.Model.Remote.FromInternet.RemoteDetails
import com.example.repechajecristianrodriguez.Model.Remote.FromInternet.RemoteList
/*
fun fromInternetItemList (concertList: List<RemoteList>): List<LocalList>{

    return concertList.map {
        LocalList(
            id = it.id,
            artista = it.artista,
            fecha = it.fecha,
            lugar = it.lugar,
            ciudad = it.ciudad,
            imagen = it.imagen,
            entradas = it.entradas
        )
    }
}

fun fromInternetItemDetails(detailsConcert: RemoteDetails): LocalDetails {

    return LocalDetails(
        id = detailsConcert.id,
        artista = detailsConcert.artista,
        fecha = detailsConcert.fecha,
        lugar = detailsConcert.lugar,
        ciudad = detailsConcert.ciudad,
        imagen = detailsConcert.imagen,
        entradas = detailsConcert.entradas

    )
}
*/

